{
  "name": "suivi-tabac-api",
  "version": "1.0.0",
  "description": "API Azure Functions pour suivi du tabac",
  "main": "dist/functions/*.js",
  "scripts": {
    "start": "func start",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/cosmos": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
